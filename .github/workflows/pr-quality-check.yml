name: PR — Quality Checks

on:
  pull_request:
    branches: [main, develop]
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: pr-quality-check-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  quality-checks:
    name: Quality / ${{ matrix.check.name }}
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        check:
          - name: Lint
            command: npm run lint:check
          - name: Format
            command: npm run format:check
          - name: Types
            command: npm run types:check
          - name: Build
            command: npm run build:check
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        if: matrix.check.name == 'Types' || matrix.check.name == 'Build'
        run: npm run prisma:generate

      - name: Run ${{ matrix.check.name }} check
        run: ${{ matrix.check.command }}
